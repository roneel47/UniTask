export interface User {
  usn: string;
  role: 'student' | 'admin';
  // Add other user-related fields if needed, e.g., name, department, semester
  // name?: string;
  // department?: string;
  // semester?: number;
}
